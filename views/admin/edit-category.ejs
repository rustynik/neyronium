<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head") %>
</head>
<body>
    <div class="container">
        <form method="POST">
            
             
            <div class="form-group">
                <label for="title">Название категории</label>
                <input type="text" class="form-control" required name="title" id="title" placeholder="Название категории" value="<%= category.title %>">
            </div>  
            
            <div class="form-group">
                <label for="parentId">Родительская категория</label>
                <select id="parentId" class="form-control"  name="parentId">
                    <% categories.forEach(item => {    <!-- for all other categories -->
                        
                        const selected = item.id == category.parentId ? "selected" : null; %>
                        
                        <option value="<%= item.id %>" <%=selected %>>
                            <%= item.title || "root" %>
                        </option>
                    <% }); %>
            
                </select>
            </div>

            <div class="form-group">
                <label for="desciption">Краткое описание</label>
                <input type="text" class="form-control" required name="description" id="description" placeholder="category desciption" value="<%= category.description %>">
            </div>

            <div class="form-group">
                    <label for="contents">Подробное описание</label>
                    <textarea type="text" class="form-control" name="contents" id="contents" placeholder="содержание">
                            <%= category.contents %>"
                    </textarea>
            </div>    
    
                <div class="form-group">
                        <label for="widgetClass">Background color for your category</label>
                        <input type="text" class="form-control" name="bgColor" id="bgColor" placeholder="bg color">
                    </div>  
            
            <button type="submit" class="btn btn-primary">Сохранить</button>

        </form>
    </div>
</body>
